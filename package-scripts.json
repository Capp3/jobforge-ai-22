{
  "scripts": {
    "server": "tsx server/server.ts",
    "server:dev": "tsx watch server/server.ts",
    "dev:full": "concurrently \"npm run server:dev\" \"npm run dev\"",
    "build:server": "tsc server/server.ts --outDir dist/server --target es2020 --module commonjs --moduleResolution node --allowSyntheticDefaultImports",
    "start:prod": "node dist/server/server.js"
  }
}